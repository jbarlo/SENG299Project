

//Handles initialization of various interface components
function connect(colour, extra){
	var http = require('http');
	var address = extra[0];
	var id = extra[1];
	
}

//Sends your move, waits for opponents move
function getMove(board, x, y, c, cb){
	
}

module.exports = {
	getMove : getMove,
    connect : connect
}